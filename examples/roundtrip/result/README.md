This is the flattened composed example, including its derived opinions and presentation; open it from the example directory with `usdview result/example.usdc` (no family plugins or sibling checkouts needed). The example's own authored layers are `layers/inputs/cameras.usda`, `layers/out/presentation.usda`, `layers/out/replay/session/derived.usda`, `layers/out/replay/session/diagnostics.revit.usda`, `layers/out/replay/session/intent.usda`, `layers/out/replay/session/kind.usda`, `layers/out/replay/session/result.revit.usda`, `layers/out/replay/session/stage.usda`, `layers/out/seed.usda`. Source pin: `usdaeco-datacentre v0.4.6`, variant `base`; source mode `pinned` (a minimal or override run does not prove the pinned release). The parent manifest records source hashes; pinned source layers are not copied here. `vanilla.png` records a stock USD render of this result.
